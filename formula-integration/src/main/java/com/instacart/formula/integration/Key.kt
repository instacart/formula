package com.instacart.formula.integration

/**
 * A [Key] is an entry in the [BackStack]. It provides essential information
 * that is necessary to instantiate a UI interface.
 *
 * Example:
 * ```
 * data class ItemKey(val itemId: String): Key
 * ```
 */
interface Key

